# Python
.venv/
__pycache__/
*.pyc

# DVC
.dvc/tmp/
.dvc/cache/

# MLflow (file store: runs + registry under mlruns/; we use remote when we move to SageMaker)
mlruns/
data/mlflow_run_id.txt

# OS/editor
.DS_Store

# Documentation files (personal notes, not for GitHub)
docs/
Guideliness.txt
current_progress.txt
reports/*.md
GITIGNORE_EXPLANATION.md
# Keep official README files
!README.md
!etl/README.md
!sql/README.md

# Data (never store big data in Git)
data/

# Docker build artifacts
*.tar
*.tar.gz
*.zip

# Lambda deployment packages
infra/lambdas/*/*.tar
infra/lambdas/*/*.zip

# Environment variables (NEVER commit secrets!)
.env
.env.local
.env.*.local
*.env

# Jupyter notebooks (testing/experimental)
src/model/testing.ipynb
src/model/test_mlflow_load.py

# Model artifacts (tracked by DVC pipeline output; do not track in Git)
models/

# CatBoost training artifacts (auto-generated, not needed)
catboost_info/

# Reports (generated files - ignore all generated content)
reports/figures/
reports/tables/
reports/eda/
reports/*.md
reports/*.csv
!reports/.gitkeep
/models
